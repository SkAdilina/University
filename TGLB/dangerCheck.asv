function value=dangerCheck(node)
state=node;
value=0;
    if state(1)==0 || state(2)==0 || state(3)==0
        if state(1)==state(2)
        value=1;
        else if state(2)==state(3)
        value=1;
            end
    end
end